export interface CancelStopRequest {
  /**
   * Add fields with @TJS annotations here
   */

  /**
   * @TJS-pattern ^\d
   */
  example: string;
}

export interface CancelStopResponse {
  /**
   * Add fields with @TJS annotations here
   */

  /**
   * @TJS-pattern ^\d
   */
  example: string;
}
